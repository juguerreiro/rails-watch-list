  <div id="banner">
    <h1>cinema</h1>
    <p>We bring tech skills to creative people</p>
  </div>


  <div class="container">
    <h2>My Lists:</h2>
    <p class=".btn .btn-primary"><%= link_to 'Create a movie list', new_list_path %></p>

    <% @lists.each do |list| %>
      <div class="card" style="width: 18rem;">
      <img src="" class="card-img-top" alt="...">
      <div class="card-body">
        <p class="card-text"><%= link_to list.name, list_path(list) %></p>
      </div>
    <% end %>
  </div>

  </div>
